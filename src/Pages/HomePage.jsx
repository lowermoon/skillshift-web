

export default function HomePage({ title }) {
    document.title = `Skillshift - ${title}`;
    return (
        <div className="">
            <h1 className="font-bold text-2x">
            </h1>
        </div>
    )
}